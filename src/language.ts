export interface AppTextTranslator {
    navigation: {
        homeText: string,
        recordsText: string,
        settingsText: string
    },
    balance: {
        currentBalanceText: string,
        incomeText: string,
        expanseText: string
    }
    walletCard: {
        ButtonText: string
    },
    walletManage: {
        TypographyText: string
    }
}