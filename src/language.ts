export interface AppTextTranslator {
    navigation: {
        homeText: string,
        recordsText: string,
        settingsText: string
    },
    ballance: {
        currentBallance: string,
        income: string,
        expanse: string
    }
}